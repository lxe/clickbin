extends layout

block body
  .bin
    h1 / #{bin.title}
    each link in bin.links
      .link
        a(href=link.url,title=link.title)
          if link.icon
            img.icon(src=link.icon)
          h3 #{link.title}